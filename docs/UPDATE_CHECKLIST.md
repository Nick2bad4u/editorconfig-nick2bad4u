# Update checklist

1. Review the EditorConfig specification and distinguish core from extension keys.
2. Resolve representative paths through every template.
3. Test overwrite refusal, force, dry-run, stdout, and the packed executable.
4. Run editorconfig-checker and inspect npm pack bin metadata.
5. Document that consumer files do not update automatically.
